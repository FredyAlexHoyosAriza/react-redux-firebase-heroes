// Aqui creamos un contexto y para ello importamos en create context
import { createContext } from "react";

// Puesto que el contexto nos devuelve un componente la variable AuthContex
// debe empezar con letra capital
export const AuthContext = createContext();